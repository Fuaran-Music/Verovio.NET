# Build output
bin/
obj/
output/

# IDE / tooling
.vs/
.vscode/
.idea/
.ionide/
.fake/

# Per-user MSBuild and tool junk
*.user
*.orig
*.fsproj.user
*.DotSettings.user

# Node modules — transient, never committed.
node_modules/
dist/

# Upstream verovio C++ clone — gitignored by Phase 04 build script.
# scripts/build-libverovio.ps1 clones rism-digital/verovio in here at the
# pinned tag, builds, and copies the DLL into runtimes/win-x64/native/.
external/

# Claude Code per-machine settings + worktrees
.claude/worktrees/
.claude/settings.local.json

# Local NuGet drop folder. Verovio.NET packs into the workspace-level
# ../local-nuget-feed/ via run.ps1, not here.
local-nuget-feed/

# Throwaway probes, log dumps from `pwsh ./run.ps1`, and intermediate
# diagnostic files. Created during phase work; never committed.
artifacts/
